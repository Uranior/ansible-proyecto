---
- name: Primer play - Ping
  hosts: pr-webserver01
  remote_user: ansible
  become: true
  tasks:
    - name: Ping all hosts
      ansible.builtin.ping:
  tags: 
    - prod
    - dev

- name: Cear archivo
  hosts: pr-webserver01
  remote_user: ansible
  become: true
  tasks:
    - name: Crear PEPE
      ansible.builtin.command:
        touch /home/ansible/PROD-pepe.txt
  tags: 
    - prod
  